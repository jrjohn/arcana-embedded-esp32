# ESP32-S3 (DNESP32S3, 16MB flash) — real A/B OTA layout.
# The shared 4MB partitions.csv has a single factory slot, so OtaService
# could never actually apply an update there. 16MB gives two 4MB app slots
# plus a flash-resident FAT volume (LCD fonts / assets / local cache later —
# mirrors the vfs partition in ALIENTEK's 16MiB example tables).
# Name,    Type, SubType,  Offset,   Size,     Flags
nvs,       data, nvs,      0x9000,   0x6000,
otadata,   data, ota,      0xf000,   0x2000,
phy_init,  data, phy,      0x11000,  0x1000,
ota_0,     app,  ota_0,    0x20000,  0x400000,
ota_1,     app,  ota_1,    0x420000, 0x400000,
vfs,       data, fat,      0x820000, 0x7E0000,
