services:
  esp32-build:
    image: espressif/idf:v5.5.2
    working_dir: /project
    environment:
      - HOME=/tmp
    volumes:
      - .:/project
    command: bash -c "rm -rf build && idf.py set-target esp32 build"
